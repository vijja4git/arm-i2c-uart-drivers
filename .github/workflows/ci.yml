name: Build & Test (ARM Driver Simulation)

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Build main firmware simulation
      run: make

    - name: Run unit tests
      run: make test